server:
  port: 8080

spring:
  application:
    name: Concurrency-lab

  # Redis ??
  data:
    redis:
      host: localhost
      port: 6379

  # Persistence ??
  datasource:
    url: jdbc:h2:file:./data/cclab;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver